import { CTA } from '@/sections/CTA';

/**
 * Única página con el bloque de contacto COMPLETO (el de las etiquetas y el
 * botón de enviar mensaje). Las demás cierran con el banner compacto.
 */
export function Contacto() {
  return <CTA />;
}
